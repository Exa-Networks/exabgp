# Minimal configuration for testing API-only mode with no BGP neighbors
# Tests that ExaBGP reactor starts and accepts API commands without any neighbor configuration

process no-neighbor-test {
	run ./run/api-no-neighbor.run;
	encoder text;
}
