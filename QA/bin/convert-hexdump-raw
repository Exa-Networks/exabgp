#!/usr/bin/env python

import sys
from struct import pack

with open(sys.argv[1],'r') as record:
	connect = record.readline()
	for line in record.readlines():
		split = line.rstrip().split()
		format = '!' +'H'*len(split)
		sys.stdout.write(pack(format,*[int(_,16) for _ in split]))
