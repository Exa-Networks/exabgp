#!/usr/bin/env python

import sys
from struct import pack

format = '!' +'H'*16

with open(sys.argv[1],'r') as record:
	connect = record.readline()
	for line in record.readlines():
		sys.stdout.write(pack(format,*[int(_,16) for _ in line.rstrip().split()]))
