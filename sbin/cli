#!/usr/bin/env python3
"""Launcher for the ExaBGP interactive CLI (the REPL mode)."""

from __future__ import annotations

import os
import sys

EXABGP = os.path.join(os.path.dirname(os.path.abspath(__file__)), 'exabgp')

os.execv(sys.executable, [sys.executable, EXABGP, 'cli'] + sys.argv[1:])
