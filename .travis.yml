language: python
python:
  - "2.6"
  - "2.7"
  - "pypy"

install:
  - true

script:
  - sbin/exabgp --version
  - set pipefail
  - ./dev/bin/selfcheck | perl -pe 's/^ +//; s/\r/\t\t/g;'
  - ./dev/bin/runtest
