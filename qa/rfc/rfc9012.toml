# RFC 9012 - The BGP Tunnel Encapsulation Attribute
# https://www.rfc-editor.org/rfc/rfc9012
#
# Every `text` below is quoted verbatim from qa/rfc/text/rfc9012.txt.
#
# Scope note.  Most of this document tells a router how to build an encapsulation header
# and which tunnel to send a packet down.  exabgp forwards no packets, so sections 7 to
# 10 do not bind it.  What binds it is section 13: the attribute is a nested TLV
# structure arriving from a peer, and section 13 says exactly what to do with each way
# it can be wrong.  This ledger is section 13 plus the two encoding rules which decide
# where a sub-TLV ends.
#
# Not recorded, for want of a keyword: section 3's "Length: 1 or 2 octets" table and the
# sentence that sub-TLV types 0-127 use a one octet length while 128-255 use two are
# written as field descriptions.  exabgp implements the split in `SubTLV.unpack_subtlvs`
# and `SubTLV.pack`; it is simply not a 2119 requirement of this document.

rfc = "rfc9012"
title = "The BGP Tunnel Encapsulation Attribute"
url = "https://www.rfc-editor.org/rfc/rfc9012"

[[requirement]]
id = "rfc9012#13-tlv-ends-with-final-subtlv"
section = "13"
level = "MUST"
status = "required"
polarity = "both"
text = """
The final octet of a TLV MUST
also be the final octet of its final sub-TLV.  If this is not the
case, the TLV MUST be considered to be malformed, and the "Treat-as-
withdraw" procedure of [RFC7606] is applied.
"""
note = """
`SubTLV.walk_subtlvs` raises `Notify(3, 1)` when a sub-TLV length does not land exactly on
the end of its Tunnel TLV, and `TunnelEncap.TREAT_AS_WITHDRAW` turns that into the
treat-as-withdraw this sentence asks for rather than the NOTIFICATION it used to become.

The rule used to be enforced only where a tunnel type decoder happened to exist, which was
tunnel type 15 and nothing else: `TunnelTypeTLV.unpack_tunnel` handed every other type to
`GenericTunnelTLV`, which keeps the value as opaque bytes, so a TLV whose final sub-TLV
ended early or late was accepted without complaint.  `unpack_tunnel` now walks the chain
for an unregistered type too.  The walk is framing only, and deliberately so: the same
section says an unrecognised sub-TLV "MUST remain in the attribute" when the route is
propagated, so nothing is read out of one and the TLV is still kept as the peer's bytes.
Splitting the walk out of `unpack_subtlvs` is what keeps the two callers on one set of
length checks instead of two that could drift apart.
"""

[[requirement]]
id = "rfc9012#13-no-valid-tlv-or-not-transitive"
section = "13"
level = "MUST"
status = "required"
polarity = "both"
text = """
If a Tunnel Encapsulation attribute does not have any valid TLVs, or
it does not have the transitive bit set, the "Treat-as-withdraw"
procedure of [RFC7606] is applied.
"""
note = """
Half right.  The empty-attribute half works by accident of the generic rule: `TunnelEncap`
does not set `VALID_ZERO`, so a zero length attribute becomes `INTERNAL_TREAT_AS_WITHDRAW`
in `AttributeCollection.parse`.  The transitive half does not.  An attribute sent
optional but not transitive fails the `Attribute.registered(aid, flag)` lookup, falls
into the `aid in Attribute.attributes_known` branch, and because `TunnelEncap` sets
neither behaviour flag it lands in the "should not happen" log and is silently dropped.
That is attribute-discard, not treat-as-withdraw: the route stays, stripped of the
attribute, where the RFC says the route must go.

This entry carries a "MUST" level because the "Treat-as-withdraw" procedure it invokes is
normative in RFC 7606; the sentence itself states the consequence rather than a keyword.
"""

[[requirement]]
id = "rfc9012#13-unknown-tunnel-type-not-malformed"
section = "13"
level = "MUST NOT"
status = "required"
polarity = "both"
text = """
If a Tunnel Encapsulation attribute can be parsed correctly but
contains a TLV whose tunnel type is not recognized by a particular
BGP speaker, that BGP speaker MUST NOT consider the attribute to be
malformed.  Rather, it MUST interpret the attribute as if that TLV
had not been present.  If the route carrying the Tunnel Encapsulation
attribute is propagated with the attribute, the unrecognized TLV MUST
remain in the attribute.
"""
note = """
`TunnelTypeTLV.unpack_tunnel` falls back to `GenericTunnelTLV`, which keeps the tunnel
type number and the raw value and re-emits both from `pack()`.  Tested from both sides:
an attribute made only of unrecognised tunnel types decodes without error, and the bytes
it packs back are the bytes the peer sent.  The negative test is the one which would
catch a decoder that dropped the unknown TLV on re-encode, which is the mistake this
sentence exists to forbid.  The third test is the boundary with the entry above: the
framing walk `unpack_tunnel` now runs over an unrecognised type checks lengths and stops,
so a sub-TLV type this implementation can decode, sitting inside a tunnel type it cannot,
still comes back out as opaque bytes rather than as a decoded preference.
"""

[[requirement]]
id = "rfc9012#13-duplicate-subtlv-first-wins"
section = "13"
level = "MUST"
status = "required"
polarity = "both"
text = """
If a Tunnel TLV has more
than one of any of these sub-TLVs, all but the first occurrence of
each such sub-TLV type MUST be disregarded.  However, the Tunnel TLV
containing them MUST NOT be considered to be malformed, and all the
sub-TLVs MUST be propagated if the route carrying the Tunnel
Encapsulation attribute is propagated.
"""
note = """
The two halves pull in opposite directions and are implemented in different places.
`SubTLV.unpack_subtlvs` still appends every sub-TLV it walks and `pack_value` still emits
every one of them, which is "MUST NOT be considered malformed" and "all the sub-TLVs MUST
be propagated".  "Disregarded" is applied in `SRPolicyTunnel.json`, which emits the first
occurrence only of each sub-TLV type RFC 9830 2.4 says must not repeat: Preference,
Binding SID, ENLP, Priority, and the two name sub-TLVs.  exabgp installs nothing, so the
only thing it can be said to interpret is what it hands the API, and the JSON is that.
Segment List and SRv6 Binding SID are not collapsed, RFC 9830 lets both repeat.
"""

[[requirement]]
id = "rfc9012#13-unrecognized-subtlv-ignored-and-kept"
section = "13"
level = "MUST"
status = "required"
polarity = "both"
text = """
If a TLV of a Tunnel Encapsulation attribute contains a sub-TLV that
is not recognized by a particular BGP speaker, the BGP speaker MUST
process that TLV as if the unrecognized sub-TLV had not been present.
If the route carrying the Tunnel Encapsulation attribute is
propagated with the attribute, the unrecognized sub-TLV MUST remain
in the attribute.
"""
note = """
`SubTLV.unpack_subtlvs` wraps an unregistered sub-TLV type in `GenericSubTLV`, which
keeps the raw value and re-emits it with the right one or two octet length field for its
type number.  The negative side is the re-encode: a `GenericSubTLV` with a type above
127 must come back with a two octet length, and one below 128 with one octet, or the
sub-TLV that "remains in the attribute" is not the one that arrived.
"""

[[requirement]]
id = "rfc9012#13-malformed-subtlv-as-unrecognized"
section = "13"
level = "MUST"
status = "required"
polarity = "both"
text = """
In general, if a TLV contains a sub-TLV that is malformed, the sub-
TLV MUST be treated as if it were an unrecognized sub-TLV.
"""
note = """
Two kinds of malformation, answered in two places.  A sub-TLV whose *framing* is wrong - a
one byte tail, a length which runs past the end of the enclosing Tunnel TLV, a type above
127 with fewer than three bytes left - still raises `Notify(3, 1)` out of
`SubTLV.unpack_subtlvs`, which `TunnelEncap.TREAT_AS_WITHDRAW` turns into treat-as-withdraw
rather than the NOTIFICATION it used to be.  That is stricter than this sentence asks, and
it is what the walk can honestly do: with the framing broken there is no way to know where
the sub-TLV ends and so no way to skip just it.  A sub-TLV whose framing is sound and whose
*value* the decoder refuses is the case this sentence really covers, and it is now handled
as written: the decoder raises `MalformedSubTLV` and `unpack_subtlvs` keeps the bytes as a
`GenericSubTLV`, out of the interpretation and into what we propagate.
`PreferenceSubTLV.unpack` is the first to do it; it used to answer `cls(0)` and tell the
API the peer had asked for preference zero.  The exception this section makes for a
malformed Tunnel Egress Endpoint sub-TLV does not arise: exabgp registers no decoder for
that sub-TLV, so it can never find one malformed.
"""

[[requirement]]
id = "rfc9012#11-must-be-able-to-filter-incoming"
section = "11"
level = "MUST"
status = "gap"
polarity = "both"
text = """
To prevent the Tunnel Encapsulation attribute from being distributed
beyond its intended scope, any BGP speaker that understands the
attribute MUST be able to filter the attribute from incoming BGP
UPDATE messages.  When the attribute is filtered from an incoming
UPDATE, the attribute is neither processed nor distributed.
"""
note = """
exabgp has no per-neighbour filter for this attribute in either direction, and no
default-on filtering for EBGP sessions as the rest of section 11 requires.  The
attribute is decoded and handed to the API for every session.  Recorded as a gap rather
than not-applicable: exabgp does understand the attribute, which is the condition the
sentence attaches to, and the configuration language is the natural place for the
filter.
"""
